/** @type {import('next').NextConfig} */
const nextConfig = {

    output: 'export',
    //distDir: 'out',
    // This is important for generating index.html
    trailingSlash: true,

};

export default nextConfig;
